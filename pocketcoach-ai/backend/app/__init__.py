"""PocketCoach AI — backend package.

Run from this directory:

    python -m uvicorn app.main:app --reload --port 8000

or from the project root (resolves the interpreter for you):

    npm run backend
"""
