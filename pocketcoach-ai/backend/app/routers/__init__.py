"""HTTP routers.

Implemented: `auth`, `users`.

The remaining modules from the product spec slot in the same way —
`/videos`, `/analysis`, `/skills`, `/drills`, `/workouts`, `/progress`, `/coach` —
each mounting under `settings.api_prefix` in `app.main`.
"""
